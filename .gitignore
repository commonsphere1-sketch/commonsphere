# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
dist-ssr/
build/
*.local

# Environment variables — NEVER commit secrets
.env
.env.*
.env.local
.env.*.local
*.env
*.env.*

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor / OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
.vscode/settings.json
.idea/

# TypeScript
*.tsbuildinfo

# Test coverage
coverage/

# Local Claude Code config — launch.json hardcodes a machine-specific
# node.exe path, so it should not be shared
.claude/

# ...but the template is meant to be shared; it holds no values.
!.env.example

# Agent skills installed via `npx skills add` — machine-local tooling
.agents/
skills-lock.json
