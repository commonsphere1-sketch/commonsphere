/**
 * globalMetricsData.ts
 *
 * NOTE: This file was committed truncated — its contents were lost and only an
 * unterminated block comment remained, which broke `tsc`. Nothing imports it.
 * Restore the real data here, or delete the file.
 */
export {};
